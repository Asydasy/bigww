node_modules/
dist/
build/
.env
.env.*
*.log
.DS_Store
Thumbs.db
.idea/
.vscode/
*.bundle
*.tar.gz

# backend
server/node_modules/
server/.env
server/.env.test

# przyklad konfiguracji MA byc w repo
!server/.env.example

# Okladki gier pobierane skryptem (cd server && npm run covers).
# Nie trzymamy ich w repo: to kilka MB binariow i cudza wlasnosc.
img/games/

# Muzyka do radia (audio/party.mp3) — tak samo jak okladki: kilka MB
# cudzej tworczosci. Repo trzyma kod, nie biblioteke dzwiekow. Bez pliku
# panel radia po prostu sie nie pokazuje.
audio/
